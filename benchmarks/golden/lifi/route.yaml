expected_path:
  - get-chains
  - get-routes
  - get-step-transaction
allowed_tools:
  - get-chains
  - get-routes
  - get-step-transaction
baseline_tokens: 10000
expected_data:
  route.steps:
    min: 1
policy:
  forbidden_actions:
    - execute_swap
