expected_path:
  - get-chains
  - get-tokens
  - get-quote
allowed_tools:
  - get-chains
  - get-tokens
  - get-quote
baseline_tokens: 8000
expected_data:
  quote.amount_out:
    min: 1
  quote.route_id:
    contains: "lifi"
policy:
  forbidden_actions:
    - execute_swap
    - transfer
